name: Run C-RANK Elite Bot

on:
  schedule:
    - cron: '30 14 * * 1-5' # 16:30 שעון ישראל (פתיחת מסחר - דוח אליט)
    - cron: '50 14 * * 1-5' # 16:50 שעון ישראל (סיגנל זהב בזמן אמת)
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install yfinance requests pandas pytz

      - name: Run bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python daily_bot.py

      - name: Commit and push journal
        run: |
          git config --global user.name "C-RANK Bot"
          git config --global user.email "bot@c-rank.com"
          git add trade_journal.csv || echo "No changes"
          git commit -m "Update Journal with 7h offset [skip ci]" || echo "No changes"
          git push || echo "Push failed"
